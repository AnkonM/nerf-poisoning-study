# Condition C3 — see docs/METHODOLOGY.md §5
defaults:
  - ../base.yaml
  - ../scenes/final_scene.yaml

condition_id: C3
poisoning:
  budget_percent: 20           # percent of V_target, per METHODOLOGY.md §2
  attack_type: hard_erasure     # hard_erasure | soft_suppression
  view_selection: random         # random | strategic
  mask_dilation_px: 4             # fixed once across the whole study
  alpha: null                      # only used when attack_type == soft_suppression

seeds: [1, 2, 3]                     # N=3 minimum, see DECISION_LOG.md D-007
