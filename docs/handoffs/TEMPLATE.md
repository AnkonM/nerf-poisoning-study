# Handoff — <date, phase-status slug>

> **This file is an INDEX/SUMMARY only.** It exists to give a fresh AI
> conversation (or a human) fast orientation — it is never the
> authoritative source for anything. The authoritative sources are always:
> **`README.md`, `docs/ROADMAP.md`, `docs/METHODOLOGY.md`, and
> `docs/DECISION_LOG.md`.**
>
> **Before doing any work, read `README.md`, `docs/ROADMAP.md`,
> `docs/METHODOLOGY.md`, and `docs/DECISION_LOG.md` in full. This file is
> a map, not a substitute.** If anything here conflicts with those files,
> those files win — this one may simply be stale.

## Project one-line summary

(One sentence. Link to [README.md](../../README.md) for the real
description — do not re-explain the study here.)

## Hardware / environment summary

(Laptop/GPU model, WSL2 + `uv`, native-Windows Blender — one or two lines
of orientation only. Link to
[environment/SETUP.md](../../environment/SETUP.md) for the actual setup
steps and verification commands — do not duplicate them here.)

## Current phase + status

(Pull this live from `docs/ROADMAP.md`'s status tracker table at the time
this handoff is written — copy the relevant row(s), don't hardcode a phase
number or description here that will silently go stale. State the date
this handoff was written.)

## Key locked decisions relevant to picking up work right now

(A short bullet list of `DECISION_LOG.md` entry IDs worth knowing about
before touching anything — not the full decision text. e.g. "D-002 —
poisoning budget is defined over target-visible views, not all views."
Pick the ones that would cause a real mistake if missed; don't list all of
them.)

## Known past issues/bugs already fixed

(Brief, with entry IDs, so they aren't rediscovered or misdiagnosed as new
problems. e.g. "D-011/D-014 — a literal brace-glob directory bug appeared
twice in the original scaffold, under src/ and data/; both fixed.")

## Immediate next steps

(What phase/task comes next per `docs/ROADMAP.md` — a short pointer to the
next phase's objective and tasks, not a copy of the whole phase section.)

---

**Before doing any work, read `README.md`, `docs/ROADMAP.md`,
`docs/METHODOLOGY.md`, and `docs/DECISION_LOG.md` in full. This file is a
map, not a substitute.**
